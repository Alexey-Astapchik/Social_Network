let initialState = {
    friendsInfo: [
        {id: 1, name: 'Bob'},
        {id: 2, name: 'John'}, 
        {id: 3, name: 'Harvey'}
    ]
};

const sidebar_reducer = (state = initialState) => {
    
    return state;
}

export default sidebar_reducer;